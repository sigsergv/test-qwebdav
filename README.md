Test webdav environment.

What we do need to implement:

  * list directory contents;
    * fetch item information;
    * correct processing of unicode filenames;
  * delete directory item;
  * upload directory item;
  * handle timeouts;
